version: '3.7'

services:
  react-boilerplate:
    build: .
    ports:
      - 5000:5000
    container_name: react-boilerplate
    stdin_open: true
    volumes:
      - './:/app'
      - '/app/node_modules'
