# react-boilerplate
Production quality custom REACT application using webpack5

## How to use


## Features


## Upcomming features



## Steps to recreate this project.
The steps have been broken down in multiple phases to add support for different features.

## Phase 1
In Phase 1, We set up only the bare essentials for webpack, typescript and react.

1. Initalize package.json

            ```npm init --y```

---

---

1. Add packages (react / webpack) 
    ```npm i -D @babel/core @babel/preset-env @babel/preset-react @babel/preset-typescript @types/node @types/react @types/react-dom babel-loader html-webpack-plugin terser-webpack-plugin typescript webpack webpack-cli webpack-dev-server```

    ```npm i react react-dom react-router-dom```

1. Create the follwing files:

    1. 
    1. src/index.tsx



